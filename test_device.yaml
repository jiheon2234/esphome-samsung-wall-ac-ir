esphome:
  name: samsung-wall-ac
  friendly_name: Samsung Wall AC

esp8266:
  board: esp8285
  restore_from_flash: true

logger:

api:

ota:
  platform: esphome

wifi:
  ssid: "testNetwork"
  password: "testPassword"

  ap:
    ssid: "samsung-wall-ac-fallback"
    password: "fallback111"

captive_portal:

external_components:
  - source:
      type: local
      path: components
    components: [samsung_wall_ac]

remote_transmitter:
  id: ir_tx
  pin:
    number: GPIO4
  carrier_duty_percent: 50%

climate:
  - platform: samsung_wall_ac
    name: "Samsung Wall AC"
    transmitter_id: ir_tx
