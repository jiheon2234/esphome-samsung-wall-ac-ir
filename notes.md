## 정리

지금 이 프로젝트에서 다루는 삼성 벽걸이 에어컨 IR 데이터는 크게 아래 4부분이다.

| 구간 | Pronto 값 | 의미 |
|---|---:|---|
| Pronto 헤더 | `0000 006D 006A 0000` | Pronto 포맷용 메타데이터 |
| IR 헤더 | `015C 00AE` | 시작 신호. 약 `9132µs mark + 4579µs space` |
| IR 데이터 | 13바이트 | 각 비트를 `BIT_MARK + ZERO_SPACE/ONE_SPACE`로 전송 |
| 종료 신호 | `0015 03C2` 근처 | 마지막 `BIT_MARK + FINAL_SPACE` |

여기서, Pronto 헤더, IR헤더, 종료신호는 고정이고 IR데이터만 동적으로 넣으면 됨


## Pronto 헤더

`0000 006D 006A 0000`

- `0000`: learned/raw Pronto format
- `006D`: carrier frequency word
  - `0x006D = 109`
  - Pronto 시간 단위는 `109 × 0.241246µs ≈ 26.30µs`, 주파수는 `1 / 26.30µs ≈ 38kHz`  
- `006A`: 첫 번째 데이터 구간의 burst pair 개수
  - `0x006A = 106`이므로, 이 뒤에 MARK/SPACE 106쌍이 들어감  (IR 헤더 1쌍, 데이터 104쌍, 종료 1쌍)
- `0000`: repeat 구간의 burst pair 개수
  - `0x0000 = 0`이므로 없음


## IR 헤더

`015C 00AE` HEADER_MARK / HEADER_SAPCE

- `015C` : 0x015C = 348이므로, 348 * 26.3µs ≈ 9132µs동안 IR송신
- `00AE` : 0x00AE = 174이므로, 174 * 26.3µs ≈ 4579µs 동안 IR 끔


## 데이터

동적으로 보내는 데이터 비트. 
`BIT_MARK` (0015) 후에 나오는 `ZERO_SPACE` | `ONE_SPACE로` 0과 1을 표현

- `0015` : `0x15` = 21이므로, 21 * 26.3µs ≈ 552µs 동안 IR 송신
- `0015 0016` = 0  (22 * 26.3µs  ≈ 579동안 IR 끔)
- `0015 0042` = 1  (66 * 26.3µs  ≈ 1737동안 IR 끔)


### 구성
IR 뒤에는 총 13바이트의 데이터가 들어가며, 1비트당 한 쌍 (0015 ????)으로 표현되므로, 총 8 * 13 = 104쌍

17도 팬1로 예시를 들어보자

`C3 4F E0 00 60 00 20 00 00 20 00 00 92`

| 위치 | 값 | 의미 |
|---:|---|---|
| `b0` | `C3` | 고정값 |
| `b1` | `4F` | **온도 바이트 = 17도** |
| `b2` | `E0` | 고정값 |
| `b3` | `00` | 고정값 |
| `b4` | `60` | **팬 LOW**|
| `b5` | `00` | 고정값 |
| `b6` | `20` | 고정값 |
| `b7` | `00` | 고정값 |
| `b8` | `00` | 고정값 |
| `b9` | `20` | 고정값 |
| `b10` | `00` | 고정값 |
| `b11` | `00` | *미확인* |
| `b12` | `92` | **체크섬** |



#### 온도 공식 (b1)

16도를 기준으로 시작하고, 온도가 1도 올라갈 때마다 0x08씩 증가

```text
0x47 + (온도 - 16) * 0x08
```

#### 팬 공식 (b4)
```text
팬1 LOW    = 0x60
팬2 MEDIUM = 0x40
팬3 HIGH   = 0x20
```


#### 미확인
캡쳐에서 서로 다르게 나오나, 00이던 01이던 동일하게 작동하는것 확인


#### 체크섬 공식(b12)
앞의 12바이트를 모두 더한 뒤, 하위 1바이트만 사용

`C3 4F E0 00 60 00 20 00 00 20 00 00 92`

```
C3 + 4F + E0 + 00 + 60 + 00 + 20 + 00 + 00 + 20 + 00 + 00 = 0x292
0x292 & 0xFF = 0x92 
```
