name: Build Check

on:
  push:
  pull_request:

jobs:
  esphome:
    runs-on: ubuntu-latest

    steps:
      - uses: actions/checkout@v4

      - name: Build ESPHome
        uses: esphome/build-action@v6
        with:
          yaml-file: test_device.yaml
